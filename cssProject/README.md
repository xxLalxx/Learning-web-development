<p>Q1. Create a normal table with 5 rows of data. The table should include Id, Product Name and Product Price as its columns. Use CSS to style the table by

a. Giving proper borders to the table and its cells.

b. Providing grey background colour to the cells.

c. Providing white colour to the text within the cells.
</p>

<p>Q2. Create a link with <a> tag. Use necessary CSS styles to make it look like a proper button instead of a link. Upon hovering over that button with mouse, show an indication to the user by using the css opacity property
</p>
  
<p>Q3. Create and design the responsive following login page using media query. The image shows how the page should look in laptop and mobile screens.</p>
